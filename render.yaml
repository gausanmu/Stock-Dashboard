services:
  - type: web
    name: nse-quant-engine
    env: docker
    region: ohio
    plan: free
    dockerCommand: "uvicorn server:app --host 0.0.0.0 --port 8000"
    envVars:
      - key: PORT
        value: 8000
